name: Build Trascrivimelo APK

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic build temporarily paused while switching ASR engine."
